# v0.1.7-enhance-context-graph Do

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-context-graph | v0.1.7 | 완료 | 2026-09-05 | 2026-09-06 |

---

## 1. 배치 현황

| ID | 모듈 | 하는 일 | 상태 | 세션 |
|---|---|---|---|---|
| B-1 | axis-server | `activeAt` · `lastA` · `promptMarks` · 시리즈/브레이크의 `a`. 픽스처 셋과 테스트 둘 | 검증 완료 | 1 |
| B-2 | axis-chart | `X`를 `a` 기반으로 교체 · 클램프 `CX` · 왼쪽 시각 라벨 자리 · 카드 제목 · `drawChart` 시그니처와 호출부 3곳 | 검증 완료 | 2 |
| B-3 | break-palette | `CAUSE_GROUPS` 신설 · `HABITS`·`FREE_CAUSES` 파생 · `.c-*` CSS 삭제 · `breakTable` 인라인 색 · 점선 색 · 빨간점 삭제 · 범례 브레이크 항목 · `.dash` 스와치 | 검증 완료 | 2 |
| B-4 | prompt-markers | 프롬프트 점 · 범례 `.dot` 항목 · `.chartbox`와 `#charttip` · 히트 테스트 바인딩 | 검증 완료 | 2 |
| B-5 | stack-and-docs | `COMP_KEYS`·`COMP` 순서 교체 | 검증 완료 | 2 |
| B-6 | stack-and-docs | README에 그래프 읽는 법 항목 | 검증 완료 | 2 |

## 2. 구현 결정

| # | 무엇 | 어떻게 | 이유 | 관련 |
|---|---|---|---|---|
| 1 | q-2 색 스왑 뒤 SC 재판정(plan 열린질문 1 지침) | `ttl_expiry`↔`effort_change` 색 스왑 뒤 SC-11(세 점선 색이 서로 다름)·SC-12(표 글자색=점선 색)·SC-13(범례 원인별 색) 세 SC 모두 값이 아니라 "다르다/일치한다"만 요구 — 스왑 후에도 성립 판정 | plan이 do 세션 중 재수정마다 이 판정을 §2에 남기라고 지시함 | B-3, D-9, q-2 |
| 2 | q-3 시각 라벨 삭제 뒤 SC 재판정(plan 열린질문 1 지침) | SC-5("x축에 프롬프트 마커 외의 눈금이 없고 양 끝에 실제 시각 라벨이 있다")를 확인 — 전반부("다른 눈금 없음")는 여전히 맞지만 후반부("양 끝 시각 라벨이 있다")는 라벨 삭제로 문자 그대로 **불성립**. 사용자가 프롬프트 호버 툴팁이 이미 그 정보를 준다며 의도적으로 결정 | plan이 재수정마다 이 판정을 §2에 남기라고 지시함. 이 건은 판정이 "불성립"이라 report의 plan 이탈로 넘어감 | B-4, D-3, q-3 |

## 3. 검증 기록

| 배치 | 수단 | 실행·확인 내용 | 결과 | 날짜 |
|---|---|---|---|---|
| B-1 | 자동 테스트 | `node --test` | 27 passed(신규 SC-1·SC-2·SC-3·SC-16·SC-17·SC-18 포함, 기존 21건 포함), 0 failed | 2026-09-05 |
| B-2 | 자동 테스트 | `node --test` (회귀 확인, B-2엔 신규 SC 자동 테스트 없음) | 27 passed, 0 failed | 2026-09-05 |
| B-2 | 사용자 육안 | `node server.js --dir data/fishing --port 7799`, 세션 `[bkit] refine-fishing-presentation`(id 2cacf362…, gaps=12 · compacts=1). SC-4(넓은 평지 없음) · SC-5(양 끝 시각 라벨만) · SC-21(제목이 활성시간 축 문구) · D-16(왼쪽 끝 회색 시스템 층에서 선이 올라옴) | 확인(사용자, "B-2 승인") | 2026-09-05 |
| B-3 | 자동 테스트 | `node --test` (회귀 확인, B-3엔 신규 SC 자동 테스트 없음) | 27 passed, 0 failed | 2026-09-05 |
| B-3 | 사용자 육안 | 같은 서버(포트 7799), 세션 `[bkit] enhance-lifting-feedback`(id a9e8…, effort_change·ttl_expiry·model_switch·compact 혼재). SC-9(빨간점 완전 삭제) · SC-10(브레이크 자리 점선만) · SC-11(세 점선 색이 서로 다름) · SC-12(표 글자색=점선 색) · SC-13(범례 원인별 색). 확인 도중 사용자가 `ttl_expiry`↔`effort_change` 색 스왑 요청(질문 q-2) → 반영 후 재확인 | 확인(사용자, "확인") | 2026-09-05 |
| B-4 | 자동 테스트 | `node --test` (회귀 확인, B-4엔 신규 SC 자동 테스트 없음) | 27 passed, 0 failed | 2026-09-05 |
| B-4 | 사용자 육안 | `node server.js --dir ~/.claude/projects --port 7799`(실데이터, 25세션). SC-14(범례에 "내 프롬프트(입력 시점)" 항목) · SC-15(점 호버 시 `yyyy-mm-dd hh:mm`만, 본문 없음, 점선 위에서는 미표시) · SC-19(점 개수 = State 카드 prompts) · SC-20(목록 접기·창 크기 변경 후에도 점선·점·툴팁이 새 폭에 맞음) | 확인(사용자, "확인") | 2026-09-05 |
| B-4 | 사용자 육안 | 같은 서버. q-3 반영 뒤 재확인 — 그래프 양 끝 시각 라벨이 완전히 사라졌는지(D-3 개정) | 확인(사용자, "승인") | 2026-09-05 |
| B-5 | 자동 테스트 | `node --test` (회귀 확인, B-5엔 신규 SC 자동 테스트 없음. 기존 테스트는 `f.comp`를 키로 읽어 `COMP_KEYS` 순서와 무관) | 27 passed, 0 failed | 2026-09-06 |
| B-5 | 사용자 육안 | `node server.js --dir data/fishing --port 7799`, 세션 `[bkit] enhance-lifting-feedback`(id a9e8…, compacts=1). SC-7(그래프 스택에서 압축 요약 층이 시스템 층 바로 위) · SC-8(Context composition 막대에서 압축 요약 조각이 시스템 조각 바로 다음) | 확인(사용자, "확인") | 2026-09-06 |
| B-6 | 사용자 육안(파일) | `README.md` 154번째 줄 — "## 숫자 읽을 때" 목록의 활성시간 항목 바로 다음에 그래프 읽는 법 항목(축·톱니·절벽·회색 시스템 층·점선·점·범례 설명) 확인 | 확인(사용자, "확인") | 2026-09-06 |

## 4. 질문 이력

| ID | 요지 | 결론 | 반영 위치 |
|---|---|---|---|
| q-1 | B-2 육안 검증 중 그래프 선을 스무딩할지(design D-4는 "직선으로 잇는다"만 정하고 스무딩 여부는 갈림길로 명시 안 함) | a(직선 유지, design D-4 그대로) — 사용자 승인 | Do §5 세션 2, design 변경 없음 |
| q-2 | B-3 육안 검증 중 `ttl_expiry`↔`effort_change` 색 스왑 요청 — plan FR-7 "기존 5색 유지"와 충돌 | b(스왑) — SC-11·SC-12·SC-13은 값이 아니라 "다르다/일치한다"만 요구해 스왑 후에도 성립, 사용자 승인 | Design D-9·§2.2 개정(1.1) |
| q-3 | B-4 육안 검증 중 그래프 양 끝 시각 라벨을 아예 삭제할지 — 프롬프트 호버 툴팁과 중복이라는 사용자 판단, plan SC-5 "양 끝 시각 라벨이 있다"와 충돌 | b(삭제) — SC-5 후반부는 불성립하며 report의 plan 이탈로 기록, 사용자 명시적 결정("내 결정, 근거 남기고 없애") | Design D-3 개정(1.2) |

## 5. 진행 로그

### 세션 1 (2026-09-05)

- 시작: B-1(axis-server) 맡음. design 확정본(1.0)과 RULE.md 검증 수단 읽음. scope 인자 없어 첫 미착수 배치의 모듈(axis-server = B-1 단독) 전체가 이번 세션 범위.
- 종료: B-1 구현·검증 완료(`activeAt` 헬퍼, `lastA`·`promptMarks` 필드, 시리즈/브레이크 `a`, `detail`의 `prompts`, 픽스처 3개 + 테스트 2파일). `node --test` 27 passed. 사용자 요청으로 B-1에서 세션 종료 — B-2~B-6은 다음 세션. 다음 세션 주의점: B-2부터는 육안 검증(브라우저)이 필요하니 세션 시작할 때 서버 기동 계획을 먼저 잡을 것.

### 세션 2 (2026-09-05)

- 시작: B-2(axis-chart) 맡음. `index.html`의 `drawChart`를 `t` 축에서 `a`(활성 누적 ms) 축으로 교체.
- B-2 도중 q-1 발생(스무딩 여부) — 질문 파일 만들고 멈춤. 사용자가 a(직선 유지, design 그대로)로 답해 design 변경 없이 종결.
- B-2 사용자 육안 확인("B-2 승인") 받고 검증 완료 처리, 확인용 서버(포트 7799) 종료. B-3(break-palette) 착수.
- B-3 구현(`CAUSE_GROUPS`·`causeGroup`·`causeColor`·`HABITS`·`FREE_CAUSES` 파생, `.c-*` 삭제, `breakTable` 인라인 색, 빨간점 삭제, 범례를 drawChart 끝에서 매번 재구성). 같은 서버(포트 7799)로 육안 검증 중 q-2 발생(색 스왑) — 반영 후 사용자 확인 받아 B-3 검증 완료.
- 범위 밖 발견(다음 사이클 후보): 그래프 맨 위 윤곽선(총 컨텍스트 라인)이 호출마다 그 호출을 처리한 모델(`col[b.m]`)로 색이 바뀌어 세션 안에서 모델이 여러 번 바뀌면 선 색이 구간마다 달라 보임. plan이 "모델별 선 색(`col`) — 그대로 둔다"고 이번 사이클 범위 밖으로 이미 명시해뒀고, 사용자도 화면 보고서야 원인을 알았다며 다음 사이클로 미루기로 함. `index.html`의 `drawChart` 중 `ctx.strokeStyle = col[b.m] || '#ccc';` 부분.
- B-4 구현(`PROMPT_COLOR`·`HIT_PX`·`chartMarks`·`fmtMin` 신설, `.chartbox`/`#charttip` 템플릿, 프롬프트 점 그리기 + 범례 `.dot` 항목, `cv.onmousemove`/`onmouseleave` 히트 테스트). 실데이터(`~/.claude/projects`, 포트 7799)로 SC-14·SC-15·SC-19·SC-20 확인.
- B-4 확인 도중 q-3 발생 — 사용자가 그래프 양 끝 시각 라벨을 삭제하자고 결정(프롬프트 호버 툴팁과 중복). plan SC-5 후반부와 충돌하지만 사용자가 명시적으로 결정해 반영, design D-3 개정(1.2). 재확인 받아 B-4 검증 완료.
- B-5 구현: `server.js`의 `COMP_KEYS`와 `index.html`의 `COMP` 순서를 함께 교체(압축 요약을 시스템 바로 다음으로). B-6 구현: README "## 숫자 읽을 때"의 활성시간 항목 다음에 그래프 읽는 법 문단 추가. 압축한 세션으로 SC-7·SC-8 확인, README는 파일에서 SC-22 확인.
- 종료: B-5·B-6 구현·검증 완료로 전 배치(B-1~B-6) 검증 완료. `node --test` 27 passed 유지. 남은 것 없음. 다음 세션 주의점: 새 세션에서 `/pdca-close` 진행.

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-05 | 초안, 배치 현황 등재 | Design 확정 |
| 0.2 | 2026-09-05 | 세션 1 — B-1 구현·검증 완료 | 배치 B-1 |
| 0.3 | 2026-09-05 | 세션 2 — B-2 구현·검증 완료(q-1 반영) | 배치 B-2 |
| 0.4 | 2026-09-05 | 세션 2 — B-3 구현·검증 완료(q-2 반영, design D-9 개정) | 배치 B-3 |
| 0.5 | 2026-09-05 | 세션 2 — B-4 구현·검증 완료(q-3 반영, design D-3 개정, plan SC-5 이탈 기록) | 배치 B-4 |
| 0.6 | 2026-09-06 | 세션 2 — B-5·B-6 구현·검증 완료. 전 배치 검증 완료로 상태를 완료로 전환 | 배치 B-5·B-6 |
