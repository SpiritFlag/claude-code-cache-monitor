# v0.1.7-enhance-context-graph Analysis

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-context-graph | v0.1.7 | 완료 | 2026-09-06 | 2026-09-06 |

---

## 1. SC 판정

| SC | 기준 | do 근거 | 독립 재확인 | 판정 |
|---|---|---|---|---|
| SC-1 | 합성 픽스처(그룹 둘 사이에 30분 공백)에서 `detail` 응답으로 시리즈 각 점의 활성축 좌표를 계산할 수 있고, 마지막 점의 좌표가 스냅숏 `activeMs`와 같다 | Do §3 B-1 자동 테스트 | `node --test` 재실행 27 passed · 0 failed. `test/context-axis.test.js` 정독 — `d.series[마지막].a === s.activeMs`를 직접 단언하고, `d.series`의 모든 항목이 `a`를 갖는다(`server.js:390`). 픽스처 `axis-gap-30m.jsonl`은 00:00:15와 00:30:00 사이에 실제로 30분 공백이 있다 | 실증 |
| SC-2 | 같은 픽스처의 공백을 30분에서 90분으로 늘려도 공백 앞뒤 두 점의 활성축 간격이 변하지 않는다 | Do §3 B-1 자동 테스트 | 같은 재실행. 테스트가 두 픽스처를 각각 replay해 `series[2].a - series[1].a`를 비교하고 둘 다 10000을 단언한다. 두 픽스처는 공백 길이(30분 대 90분)만 다르고 나머지 줄이 동일함을 파일 대조로 확인 | 실증 |
| SC-3 | 픽스처의 마지막 그룹이 열린 채(뒤에 이벤트 없음)여도 그 점의 활성축 좌표가 직전 점보다 크다 | Do §3 B-1 자동 테스트 | 같은 재실행. 픽스처 마지막 줄이 assistant(`u-ag-4`)라 그 그룹은 flush 없이 열린 채로 남고, `series.push`(`server.js:390`)가 그 시점의 `activeAt`으로 좌표를 박는다. 테스트가 마지막 점 > 직전 점을 단언. 같은 값이 SC-1에서 flush 후 `activeMs`와 일치하므로 열린 그룹이 누락도 중복도 아님이 함께 잡힌다 | 실증 |
| SC-4 | 아카이브에서 5분 초과 공백이 3건 이상인 세션을 띄우면 그래프에 넓은 평지가 없다 | Do §3 B-2 사용자 육안 2026-09-05(포트 7799, 세션 `[bkit] refine-fishing-presentation`, gaps=12) | 재확인 불가(사용자 확인 신뢰). 축 식이 `a` 기반임은 코드로 확인(`index.html:443`) | 실증 |
| SC-5 | x축에 프롬프트 마커 외의 눈금이 없고 양 끝에 실제 시각 라벨이 있다 | Do §3 B-2 · B-4 사용자 육안 2026-09-05, Do §2 판정 2 | 전반부(프롬프트 마커 외 눈금 없음)는 코드로 확인 — `drawChart`에 x 방향 눈금 그리기가 없고 가로 격자선 5줄만 있다(`index.html:446`). 후반부는 `toLocaleTimeString` 호출이 `index.html`에 0건이라 라벨이 실제로 삭제됐음을 확인 | 범위 변경 — 후반부(양 끝 시각 라벨)는 q-3 사용자 결정으로 삭제, → report §4 D-3. 전반부는 실증 |
| SC-6 | 삭제 — FR-3 미채택(design D-4)으로 검증 대상 없음 | — | — | 해당 없음 |
| SC-7 | 압축한 세션의 그래프에서 압축 요약 층이 시스템 층 바로 위에 있다 | Do §3 B-5 사용자 육안 2026-09-06(포트 7799, 세션 `[bkit] enhance-lifting-feedback`, compacts=1) | 재확인 불가(사용자 확인 신뢰). 스택 순서의 출처는 코드로 확인 — `server.js:86`의 `COMP_KEYS`가 `summary`를 첫 키로 두고 `sys`가 앞에 붙으며(`server.js:390`), `index.html:333`의 `COMP`가 `sys` 다음이 `summary`다 | 실증 |
| SC-8 | 같은 세션의 구성 막대에서 압축 요약 조각이 시스템 조각 바로 다음이다 | Do §3 B-5 사용자 육안 2026-09-06 | 재확인 불가(사용자 확인 신뢰). 구성 막대도 같은 `COMP` 배열을 읽으므로 그래프와 순서가 구조적으로 같다 | 실증 |
| SC-9 | 콜드 스타트로 시작한 세션의 첫 점에 빨간점이 없다 | Do §3 B-3 사용자 육안 2026-09-05 | 코드로 재확인 — 빨간점 휴리스틱(`cw > 20000 && cw > ctx*0.3`)이 `index.html`에 0건이다. 시리즈 점에 원을 그리는 코드 자체가 남아 있지 않다 | 실증 |
| SC-10 | 브레이크 자리에 점이 없고 세로 점선만 있다 | Do §3 B-3 사용자 육안 2026-09-05 | 코드로 재확인 — `breaks` 순회는 `setLineDash([3,3])` 세로선 하나뿐이고(`index.html:470`) `arc` 호출은 프롬프트 마커 자리에만 있다(`index.html:475`) | 실증 |
| SC-11 | model_switch · effort_change · compact가 섞인 아카이브 세션에서 세 점선의 색이 서로 다르다 | Do §3 B-3 사용자 육안 2026-09-05(세션 `[bkit] enhance-lifting-feedback`, 색 스왑 후 재확인) | 코드로 재확인 — `CAUSE_GROUPS`에서 model `#ff9f43` · effort `#ffc857` · compact `#6ea8fe`로 세 값이 서로 다르다(`index.html:362-370`) | 실증 |
| SC-12 | 같은 세션의 Cache breaks 표에서 각 원인의 글자색이 그래프 점선 색과 같다 | Do §3 B-3 사용자 육안 2026-09-05 | 코드로 재확인 — 표(`index.html:434`)와 점선(`index.html:470`)이 같은 `causeColor(b.cause)`를 부르고 `.c-*` CSS 규칙은 0건이라 다른 출처가 없다 | 실증 |
| SC-13 | 범례에 브레이크 점선 항목이 원인별 색으로 있다 | Do §3 B-3 사용자 육안 2026-09-05 | 코드로 재확인 — 범례가 그 세션 `breaks`에 나온 묶음만 모아 `g.color`로 `.dash` 스와치를 만든다(`index.html:481-485`) | 실증 |
| SC-14 | 범례에 프롬프트 마커 항목이 있다 | Do §3 B-4 사용자 육안 2026-09-05 | 코드로 재확인 — `prompts`가 비어 있지 않으면 `.dot` 스와치와 "내 프롬프트(입력 시점)" 항목이 붙는다(`index.html:486`) | 실증 |
| SC-15 | 프롬프트 점에 마우스를 올리면 `yyyy-mm-dd hh:mm` 형식의 실제 시각이 툴팁으로 보이고 본문은 보이지 않는다. 브레이크 점선 위에서는 툴팁이 뜨지 않는다 | Do §3 B-4 사용자 육안 2026-09-05(실데이터 25세션) | 재확인 불가(사용자 확인 신뢰). 히트 테스트가 `chartMarks.prompts`만 순회하고(`index.html:323`) 툴팁 내용이 `fmtMin(best.t)` 하나임은 코드로 확인. 형식은 `fmtMin`이 `yyyy-mm-dd hh:mm`으로 조립한다(`index.html:343`) | 실증 |
| SC-16 | 합성 픽스처(사람 프롬프트 2 · `isMeta` user 1 · 툴 결과 user 2)에서 `detail`의 프롬프트 목록 길이가 2이고 스냅숏 `prompts`와 같다 | Do §3 B-1 자동 테스트 | `node --test` 재실행. 픽스처 `prompt-marks.jsonl`이 사람 프롬프트 2(`u-pm-0` 문자열 content · `u-pm-6` text 블록) · `isMeta` 1(`u-pm-2`) · 툴 결과 2(`u-pm-3` · `u-pm-5`)로 plan이 요구한 구성과 정확히 같음을 파일에서 확인. 테스트가 길이 2와 `s.prompts` 일치를 둘 다 단언 | 실증 |
| SC-17 | 같은 픽스처의 프롬프트 목록 각 항목의 시각이 사람 프롬프트 user 줄의 시각과 같다 | Do §3 B-1 자동 테스트 | 같은 재실행. 테스트가 단언하는 두 시각이 픽스처 `u-pm-0`(00:00:00) · `u-pm-6`(00:00:30)의 timestamp와 일치함을 대조 | 실증 |
| SC-18 | 같은 픽스처에서 `JSON.stringify(detail(id))`에 픽스처 프롬프트 본문 문자열이 없다 | Do §3 B-1 자동 테스트 | 같은 재실행. 테스트가 `prompt-body-one` · `prompt-body-two` 두 문자열의 부재를 단언하고, 그 문자열이 픽스처의 실제 본문임을 대조 | 실증 |
| SC-19 | 실사용 세션의 그래프에 프롬프트 점이 있고, 개수가 State 카드의 prompts 값과 같다 | Do §3 B-4 사용자 육안 2026-09-05(실데이터 `~/.claude/projects`) | 재확인 불가(사용자 확인 신뢰). 개수가 어긋날 두 경로는 코드로 막혀 있다 — `detail`이 `promptMarks`를 자르지 않고 통째로 내리며(`server.js:460`), 축 밖 좌표는 `CX`가 클램프해 점이 사라지지 않는다(`index.html:444`) | 실증 |
| SC-20 | 세션 목록을 접거나 창 크기를 바꾼 뒤에도 점선 · 점이 새 폭에 맞게 다시 그려지고, 프롬프트 점의 툴팁이 새 자리에서 뜬다 | Do §3 B-4 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 재그리기 경로 셋이 모두 `d.prompts`를 넘기고(`index.html:329` · `521` · `524`), `drawChart`가 매번 `chartMarks`를 새로 채우므로(`index.html:472`) 좌표가 낡을 경로가 없음을 코드로 확인 | 실증 |
| SC-21 | 그래프 카드 제목이 축이 활성시간임을 말한다 | Do §3 B-2 사용자 육안 2026-09-05 | 코드로 재확인 — 제목이 `Context over time (x축 = 활성시간 누적 · 유휴 잘라냄 — 톱니 = 캐시 재작성, 절벽 = 압축)`이다(`index.html:308`) | 실증 |
| SC-22 | README에 그래프의 축 · 점선 · 점 · 범례를 읽는 문단이 있다. 확인 대상은 파일이다 | Do §3 B-6 사용자 육안(파일) 2026-09-06 | 파일 직접 확인 — `README.md:155`가 활성시간 항목 바로 다음에 있고 축 · 왼쪽 회색 층 · 세로 점선과 원인별 색 · 프롬프트 점과 툴팁 · 범례를 모두 다룬다 | 실증 |
| SC-23 | 기존 테스트 21건이 계속 통과한다 | Do §3 B-1~B-5 자동 테스트 | `node --test` 재실행 27 passed · 0 failed. 신규 6건(SC-1 · SC-2 · SC-3 · SC-16 · SC-17 · SC-18)을 뺀 21건이 기존 테스트이고 모두 통과 | 실증 |

## 2. 결정 이행

| D | 결정 | 지켰나 | 어긋남 |
|---|---|---|---|
| D-1 | (a) 시리즈 점 · 브레이크 · 프롬프트 각각에 활성 누적 ms `a`를 서버가 붙인다 | 예 | `server.js:390`(series) · `364`(breaks) · `299`(promptMarks) 셋 다 `activeAt`으로 `a`를 받는다 |
| D-2 | 레코드를 먹는 자리에서 `s.activeMs + (열린 그룹이면 ts - grp.prevTs)`로 낸다 | 예 | `server.js:222-225`가 설계 §2.2의 식과 문자 그대로 같고, 단조 고정 `Math.max`까지 포함한다. `flushGroup` 재호출 없음 |
| D-3 | (b) 정의역 `[0, series[마지막].a]`. 양 끝 시각 라벨은 q-3으로 그리지 않는다 | 예 | `index.html:443`이 `X = L + (W-L-R) * a / max(1, a1)`로 0을 왼쪽 끝에 둔다. `toLocaleTimeString` 0건으로 라벨 삭제 확인. 이 개정이 plan SC-5 후반부를 깨뜨린 것은 설계가 스스로 적어둔 대로다 |
| D-4 | 절단 표시를 두지 않는다 | 예 | 점과 점을 그대로 잇는다(`index.html:469`). 절단 표시 코드 없음 |
| D-6 | (a) 프롬프트 마커는 x축 바닥선 위 | 예 | `index.html:474`가 `y = Y(0)` |
| D-7 | 프롬프트 · 브레이크의 x를 `[L, W-R]`로 클램프 | 예 | `index.html:444`의 `CX`를 점선(`470`)과 프롬프트 점(`474`)이 함께 쓴다 |
| D-8 | `CAUSE_GROUPS` 하나가 점선 · 범례 · 표의 색을 모두 준다. `.c-*` CSS는 삭제하고 표는 인라인 style | 예 | `.c-` CSS 규칙 0건. 표(`434`) · 점선(`470`) · 범례(`481-485`)가 전부 `causeColor` · `causeGroup` 경유. `HABITS` · `FREE_CAUSES`도 같은 배열에서 파생한다(`375-376`) |
| D-9 | 새 3색(`session_resume` 청록 · 툴 목록 분홍 · `breakpoint_shift` 저채도 회청)과 q-2 스왑(`ttl_expiry` 빨강 · `effort_change` amber) | 예 | `index.html:362-370`의 값이 설계와 일치. FR-7의 "기존 5색 유지"에서 이 둘만 벗어나며 do §2가 SC-11 · SC-12 · SC-13 성립을 판정해뒀다 |
| D-10 | 범례에는 그 세션 `breaks`에 실제로 나온 묶음만 | 예 | `index.html:481`이 `seenKeys`로 등장한 묶음만 모은다 |
| D-11 | 범례 스와치에 `.dash`(3×11) · `.dot`(9px 원) 변형 | 예 | `index.html:116-117` |
| D-12 | `.chartbox` 안 절대배치 `div#charttip`. `drawChart`가 좌표를 `chartMarks`에 남기고 히트 테스트가 그것만 본다 | 예 | `index.html:118-119`(CSS) · `308`(DOM) · `472-477`(좌표 적재) · `320-328`(히트 테스트) |
| D-13 | 서버 `COMP_KEYS`와 화면 `COMP` 순서를 함께 바꾼다 | 예 | `server.js:86`과 `index.html:333`이 둘 다 `summary`를 앞으로 옮겼고, 인덱스로 읽는 결합은 그대로다 |
| D-14 | 프롬프트 마커 목록에 상한을 두지 않는다 | 예 | `server.js:460`이 `prompts: s.promptMarks`로 통째로 내린다(`breaks`만 `slice(-40)`) |
| D-15 | 카드 제목 교체 · README의 활성시간 항목 바로 다음에 그래프 읽는 법 | 예 | `index.html:308` · `README.md:155` |
| D-16 | 축 왼쪽 빈 구간을 회색 시스템 층으로 메운다 | 예 | `index.html:453-456`이 `series[0].a > 0`일 때만 `a = 0`에 시스템 몫만 담은 선두 점을 앞에 붙이고, 그 배열은 스택 · 선 · 채우기에만 쓴다. 브레이크 · 프롬프트 · 범례는 원래 `series`를 본다 |

## 3. 결함

감사 중 발견한 결함이 없다.

확인한 것 중 결함으로 보였다가 아닌 것: `drawChart`의 이른 반환(`!series.length`)은 `chartMarks`를 리셋하지 않아 이전 세션의 프롬프트 좌표가 남을 수 있으나, 세션 목록이 `s.calls > 0`만 내리고(`server.js:435`) 호출이 있으면 시리즈 점도 최소 하나라 도달할 수 없는 경로다.

## 4. 감사 방법

- 직접 실행한 것: `node --test`(27 passed · 0 failed, 2026-09-06). `test/context-axis.test.js` · `test/prompt-marks.test.js` 정독과 픽스처 3개(`axis-gap-30m` · `axis-gap-90m` · `prompt-marks`) 대조. `server.js`의 `activeAt` · `session()` 초기값 · `COMP_KEYS` · `user`/`assistant` 분기 · `detail` 대조. `index.html`의 `CAUSE_GROUPS` · `drawChart` 전문 · 히트 테스트 · 범례 · CSS 대조. 삭제 확인은 부재 검색으로 했다(`cw > 20000` · `.c-` CSS · `toLocaleTimeString` 모두 0건). `README.md` 그래프 문단 확인.
- Do 기록을 신뢰한 것: 브라우저 화면을 실제로 본 SC(SC-4 · SC-7 · SC-8 · SC-15 · SC-19 · SC-20). 캔버스 렌더 결과라 이 세션에서 재현할 수단이 RULE.md 검증 수단에 없다. 대신 각 SC가 어긋날 수 있는 코드 경로를 대조해 구조적으로 막혀 있는지를 확인했고, 그 내용을 §1의 독립 재확인 열에 적었다.
- 감사 범위 밖: `data/fishing` 아카이브의 실측 수치(plan §3 F-1~F-13). 이 사이클이 그 수치를 바꾸지 않았고 재측정이 감사 대상이 아니다.

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-06 | 감사 | Do 전 배치 검증 완료 |
