# v0.1.8-enhance-visual-polish Analysis

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-visual-polish | v0.1.8 | 완료 | 2026-09-06 | 2026-09-06 |

---

## 1. SC 판정

| SC | 기준 | do 근거 | 독립 재확인 | 판정 |
|---|---|---|---|---|
| SC-1 | 1281px 이상 폭에서 상단 타일 4개가 고른 시안의 배치(1x4)로 그려진다 | Do §3 B-5 사용자 육안 2026-09-06 | `git diff v0.1.7..HEAD -- index.html`에서 `.top { grid-template-columns: 280px 1fr 1fr 240px }`와 두 미디어쿼리에 변경 없음 — D-16이 정한 1x4가 그대로다 | 실증 |
| SC-2 | 네 타일 각각의 내용이 타일 안에서 치우치지 않고, 사용자가 시안과 대조해 "옮겨졌다"고 인정한다 | Do §3 B-5 사용자 육안 2026-09-06(1440→1280→720, "내용 안 치우침·바닥 빈 공간 없음") | `index.html:61` `.top .card`가 네 카드 공통으로 `flex-direction:column`, 카드별 가운데 블록 `.ttl-mid`·`.ctx-mid`·`.cost-mid`·`.char-mid`가 모두 `flex:1`을 가진 것을 확인. "시안과 대조해 인정"은 사용자만 할 수 있다 | 실증 |
| SC-3 | design 문서에 네 타일의 요소 목록이 있다 | Do §3 B-5 사용자 육안 2026-09-06 | Design §2.2 끝 "네 타일의 요소 목록" 표를 직접 읽음(네 행) | 실증 |
| SC-4 | 화면의 타일 요소가 SC-3의 목록과 일치하고 더하거나 뺀 요소가 없다 | Do §3 B-5 사용자 육안 2026-09-06 | `renderMain`(288~330행) · `tick`(544~560행) · `charCard`(213~218행)을 Design §2.2 표와 한 항목씩 대조 — 네 타일 전 항목이 있고 목록 밖 요소는 없다. 삭제된 Cost 3칸 통계는 §2.2 v1.3이 이미 목록에서 뺐고, 딸린 CSS `.cost-stats`·`.cost-left`·`.cost-right`·`.today-total`도 남지 않았다 | 실증 |
| SC-5 | 1280px 이하 폭에서 Cost 타일이 넓은 열에 있다 | Do §3 B-5 사용자 육안 2026-09-06 | `index.html:143-147` `@media (max-width:1280px)`의 `280px 1fr` + 3번째 자식(Cost) `order:2` 확인, v0.1.7과 동일 | 실증 |
| SC-6 | 720px 이하 폭에서 상단 타일이 한 열로 쌓인다 | Do §3 B-5 사용자 육안 2026-09-06 | `index.html:148` `@media (max-width:720px) { .top { grid-template-columns: 1fr } }` 확인, v0.1.7과 동일 | 실증 |
| SC-7 | 상단 Context 바가 여덟 층이고 층 순서가 `COMP`와 같다 | Do §3 B-1 사용자 육안 2026-09-06 | `ctxBarSegs`가 `COMP.map`으로 그려 순서가 정의상 같다. 층 개수 여덟은 `COMP` 배열 길이 | 실증 |
| SC-8 | 상단 바의 칠해진 총 폭이 `ctx/1M`와 같다 | Do §3 B-1 로그·수치 2026-09-06 | 포트 8199로 `--dir data/fishing` 재기동, `/api/state`의 세션 `ff88ac4a-…` 실측: `ctx=799928`(79.9928%), `comp` 여덟 필드 합 `799929`(79.9929%), 차 1토큰. do의 기록과 같은 값 | 실증 |
| SC-9 | 상단 바의 굵기가 10px이고 둥근 모서리가 그대로다 | Do §3 B-1 사용자 육안 2026-09-06 | `index.html:86` `.ctxbar { height:10px; border-radius:5px }` — `.bar`와 같은 값 | 실증 |
| SC-10 | 상단 바의 각 층 색이 그래프 범례의 같은 항목 색과 같다 | Do §3 B-1 사용자 육안 2026-09-06 | 바(`ctxBarSegs`)와 그래프 스택·범례가 모두 `COMP`의 세 번째 값을 쓴다 — 색의 단일 출처가 하나 | 실증 |
| SC-11 | 범례에 여덟 층이 전부 있고(0인 층 포함) 항목 색이 바의 층 색과 같다 | Do §3 B-1 사용자 육안 2026-09-06 | `ctxLegend`가 `COMP.map`으로 여덟 행을 전부 그리고 0인 행은 지우지 않고 `.zero`만 붙인다. 실측 세션에서 `summary=0`·`images=0` 두 층이 실제로 0값 행이었다 | 실증 |
| SC-12 | `comp`가 없는 세션에서 바가 단색 한 층으로 그려지고 콘솔 오류가 없다 | Do §3 B-1 비고 — 미관측(데이터 없음) | 포트 8199 `/api/state` 41개 세션 전부 `comp`를 가지고 있어 이번에도 관측 못함. 코드 경로(`ctxBarSegs`의 `if (!c)` 단색 한 층, `ctxLegend`·`ctxNote`의 빈 문자열 반환)는 읽어 확인했으나 화면으로 본 것이 아니다 | 미실증 — 해당 세션이 아카이브·실사용 어디에도 없다 (→ report §3.2) |
| SC-13 | 모델 3구간 이상 세션에서 총 컨텍스트 윤곽선이 한 색 한 선으로 이어진다 | Do §3 B-3 사용자 육안 2026-09-06(세션 `ff88ac4a-…`, opus→fable→sonnet) | `index.html:513-517` 윤곽선이 `beginPath` 1회 · `lineTo` 반복 · `stroke` 1회이고 `strokeStyle`이 `LINE_COLOR` 고정 — 구간별 색 분기가 없다. "이어져 보인다"는 사용자 확인을 신뢰 | 실증 |
| SC-14 | 언제 어느 모델로 바꿨는지가 모델 바꾸기 점선과 Cache breaks 표의 from→to에서 읽힌다 | Do §3 B-3 사용자 육안 2026-09-06 | 재확인 불가(사용자 확인 신뢰). D-15대로 더한 표시가 없고 기존 점선·표 코드도 이번 diff에 없다 | 실증 |
| SC-15 | `index.html`에 모델별 선 색 상수(`col`)가 남아 있지 않다 | Do §3 B-3 로그·수치 2026-09-06 | `grep -n 'col\['`·`grep -n 'const col'` 모두 빈 결과 | 실증 |
| SC-16 | `node --test`가 기존 27건 전부 통과한다 | Do §3 B-2·B-3·B-5·B-6 자동 테스트 2026-09-06 | `node --test` 독립 재실행 — 27 pass / 0 fail. 이 27건은 replay 집계(브레이크 원인·비용·컴포지션 수치)를 단언하고 화면 CSS·마크업을 단언하지 않는다. 즉 SC-16은 "이번 UI 변경이 집계를 안 건드렸다"를 단언할 뿐이고, 다른 SC의 근거가 되지 않는다 | 실증 |
| SC-17 | README에 상단 Context 바 읽는 법이 있다 | Do §3 B-6 사용자 육안 2026-09-06 | README 6행 "Context 카드의 여덟 색 바·범례와 톱니 그래프로 함께 보여준다"와 150행 "Context 카드의 바와 범례가 그 구성을 보여준다" 확인 | 실증 |
| SC-18 | README의 그래프 읽는 법 문단이 선이 한 색임을 말한다 | Do §3 B-6 사용자 육안 2026-09-06 | README 154행 "총 컨텍스트 윤곽선은 모델이 바뀌어도 색이 바뀌지 않는 한 색이다" 확인 | 실증 |
| SC-19 | 범례의 항목마다 토큰과 컨텍스트 대비 비율이 적혀 있다 | Do §3 B-1 사용자 육안 2026-09-06 | `ctxLegend`가 행마다 `.cl-tk`(토큰)와 `.cl-pc`(비율)를 그리고, 비율의 분모가 여덟 층 합인 것이 D-5와 같다 | 실증 |
| SC-20 | 범례의 시스템 항목에 실측(콜드) · 실측(웜) · 추정 중 하나가 적혀 있다 | Do §3 B-1 사용자 육안 2026-09-06 | `SYS_SRC`(`index.html:399-403`)가 `resolveSys`가 내보내는 여덟 source 값을 빠짐없이 덮어 `\|\| '추정'` 폴백으로 새는 값이 없음을 대조. 실측 세션의 `sysSource`는 `cold-key` → "실측(콜드)" | 실증 |
| SC-21 | 화면에 Context composition 카드가 없다 | Do §3 B-2 사용자 육안 2026-09-06 | `grep -n 'compBar\|Context composition\|\.stack\|legend .li em'` 빈 결과 — 카드 · `compBar` · 딸린 CSS 모두 없음 | 실증 |
| SC-22 | 구성이 추정치라는 단서가 상단 바 근처(툴팁 또는 범례)에서 읽힌다 | Do §3 B-1 사용자 육안 2026-09-06 · B-5 사용자 육안 2026-09-06 | `ctxNote`가 `.ctx-mid` 안 바 **바로 위** 한 줄로 상시 그려진다(툴팁 아님). 자리는 D-8이 적은 "범례 아래"와 다르지만(I-1) 바에 붙어 상시 읽히는 것은 같다 | 실증 |

## 2. 결정 이행

| D | 결정 | 지켰나 | 어긋남 |
|---|---|---|---|
| D-1 | `.bar`를 쓰지 않고 `.ctxbar`를 새로 둔다. `.bar`는 손대지 않고 굵기 10px · 모서리 5px · 트랙 `#232836`만 옮겨 적는다 | 예 | |
| D-2 | 층 폭은 `c[k]/1e6*100` %, `<i>`에 `flex:none` | 예 | |
| D-3 | 얇은 층에 `min-width`를 두지 않는다 | 예 | |
| D-4 | 층 사이 경계선을 두지 않고 시작한다 | 예 | 층 사이 경계선은 끝까지 두지 않았다. 대신 바 **전체** 위아래에 `box-shadow:inset` 1px 두 줄을 더했다(do §2 #1) — D-4가 예고한 층별 `inset -1px 0 0`와는 다른 것이고, 지적된 문제도 색 구분이 아니라 높낮이 착시였다 |
| D-5 | 범례 비율의 분모는 여덟 층 합(≈ `ctx`) | 예 | |
| D-6 | 0인 층은 행을 남기고 `.zero`로 글자를 죽인다 | 예 | |
| D-7 | 범례는 바 바로 아래 | 예 | |
| D-8 | 추정 단서는 범례 마지막 항목 아래 `.cl-note` 한 줄 | 아니오 | B-5가 B-4 시안(`card-layout-changes.md` §2)대로 바 **바로 위**(`text-align:right`)로 올렸다. 사용자가 승인한 시안이고 SC-22도 충족되지만, do §2에도 design 개정에도 남지 않아 문서와 코드가 어긋난 채로 있었다 → I-1 |
| D-9 | `comp`가 없으면 단색 한 층, 범례·단서는 안 그린다 | 예 | 코드로만 확인. 실물 미관측(SC-12) |
| D-10 | 하단 카드와 함께 `compBar` · `.stack` · `.stack i` · `.legend .li em`을 지우고 `.legend`·`.legend .li b`·`SYS_SRC`는 남긴다 | 예 | |
| D-11 | 윤곽선 색 `#e6e8ee` | 예 | |
| D-12 | `LINE_COLOR`를 `PROMPT_COLOR` 옆 상수 블록에 둔다 | 예 | |
| D-13 | 윤곽선을 `beginPath` 1회 · `stroke` 1회 · `lineJoin='round'` | 예 | |
| D-14 | `hasComp`가 아닐 때 채우기는 `LINE_COLOR + '22'` 폴리곤 하나 | 예 | |
| D-15 | 모델 이력 표시를 더하지 않는다 | 예 | |
| D-16 | 상단 타일 배치를 그대로 둔다(`.top` 그리드·미디어쿼리 손대지 않음) | 예 | v0.1.7과 diff 없음 |
| D-17 | `design` 스킬 캔버스로 시안 두 장 | 아니오 | do가 만든 캔버스는 사용자가 폐기하고, 사용자가 직접 만든 `card-layout-changes.md`(글 명세)를 B-4 산출물로 제출했다. do §5에 기록됨. 결정한 **방법**은 안 지켜졌지만 결정의 목적(B-5 전에 타일 안 배치를 사용자와 확정)은 달성됐다 |
| D-18 | 시안 반복이 두 번 안에 안 끝나면 코드에서 다듬고 반복 횟수를 do §3에 적는다 | 해당 없음 | 시안 반복이 한 번에 끝나 발동하지 않았다 |
| D-19 | 네 카드를 `flex-direction:column`으로 통일하고 남는 높이를 흡수할 요소 하나에 `flex:1` | 예 | `.ttl-mid`·`.ctx-mid`·`.cost-mid`·`.char-mid` 넷 다 `flex:1`. 흡수 요소가 D-19가 지목한 것과 다른 카드가 있으나(Context는 "범례 블록"이 아니라 단서+바+범례를 함께 감싼 `.ctx-mid`) 규칙 자체는 지켰다 |
| D-20 | 상단 전용 클래스에 `t-` 접두사를 쓰고 공유 클래스 `.card`·`.big`·`.sub`·`.bar` 값은 바꾸지 않는다 | 부분 | 접두사는 `t-`가 아니라 시안이 정한 `ttl-mid`·`ctx-top`·`ctx-mid`·`cost-foot`·`cost-k`·`vsep`·`char-mid`를 썼다(do §2 #3, 사용자 제출 시안 이름). 결정의 목적인 "공유 클래스 값 불변"은 지켰다 — `git diff v0.1.7..HEAD`에서 `.card`·`.big`·`.sub`·`.bar` 기본 규칙에 변경이 없고, 상단에서 달라지는 값은 전부 `.top .card …`·`.cost-k .big`·`.ring .num .big`처럼 스코프됐다 |
| D-21 | 두 미디어쿼리를 그대로 둔다 | 예 | |
| D-22 | README 7행 · 150행 · 154행 | 예 | 세 자리 모두 D-22가 적은 내용으로 바뀌었고 149행은 손대지 않았다 |
| D-23 | 범례를 한 열 여덟 줄로 | 예 | 시안이 2열 그리드를 요구했으나 q-3에서 한 열 유지로 확정, `.complegend { flex-direction:column }` 확인 |

## 3. 결함

| ID | 발견 | 위치 | 처리 | 재검증 |
|---|---|---|---|---|
| I-1 | D-8은 추정 단서 줄을 "범례 마지막 항목 아래"로 정했고 do §3 B-1 육안도 "범례 아래 텍스트 유지"로 확인받았는데, B-5가 B-4 시안대로 바 **바로 위**로 올렸다. 이 뒤집힘이 q-3에서 걸러지지 않았고(q-3은 범례 열 수와 Cost 통계 두 건만 다뤘다) design 개정에도 do §2에도 남지 않았다. 화면은 사용자가 승인한 모습이 맞고 SC-22도 충족되므로 코드는 옳다 — 틀린 것은 문서다 | `docs/…design.md` D-8 · §2.2, `index.html:386` 주석 | 수정 — design D-8에 v1.4 개정 사실을 적고, §2.2 코드 블록을 실제 구현대로 `ctxLegend`/`ctxNote` 분리 · 마크업 예시에 `ctxNote(s)` 추가. `index.html`의 잘못된 출처 주석(`do §2 #3`은 클래스 이름 항목이다)을 `D-8 v1.4`로 고침 | 문서 결함이라 회귀 테스트가 붙지 않는다. 대신 design §2.2 코드 블록과 `index.html`의 `ctxBarSegs`·`ctxLegend`·`ctxNote` 세 함수를 한 줄씩 대조해 일치를 확인했고, `node --test` 27 pass / 0 fail로 주석 수정이 아무것도 깨지 않음을 확인 |
| I-2 | Design §2.2 요소 목록이 Cache TTL 링을 "180px"로 적어둔 채였다. 실제는 160px이고 do §2 #5가 그 변경을 기록했지만 design 쪽이 따라가지 않았다 | `docs/…design.md` §2.2 요소 목록 | 수정 — 요소 목록의 링 크기를 160px로 고치고 Version History 1.4에 함께 적음 | 위와 같음. `index.html:296`의 `<svg width="160" height="160" … r="70" stroke-width="11" stroke-dasharray="439.8">`와 대조해 160px·r=70·2πr=439.8이 서로 맞음을 확인 |
| I-3 | `heroCard`가 쓰는 `.stats` CSS(33~35행)가 살아 있으나 `heroCard`·`scoreCard`는 화면에 그려지지 않는 죽은 코드다 | `index.html:33-35`, `index.html:273` | 이월 → report §3.4 | 이번 사이클이 만든 것이 아니다(v0.1.7에도 같은 상태). D-10이 "죽은 `table.tools`·`.tb`는 이번 삭제 대상이 아니다"로 같은 성격의 것을 이미 범위 밖에 뒀으므로 여기서도 손대지 않는다 |

## 4. 감사 방법

- **직접 실행한 것**
  - `node --test` 재실행 (27 pass / 0 fail). 테스트 27건이 무엇을 단언하는지 이름을 읽어 화면 SC를 덮지 않음을 확인.
  - `node server.js --dir data/fishing --port 8199` 기동 → `curl localhost:8199/api/state`로 SC-8 재실측(`ctx` 799928 vs `comp` 합 799929) 및 41개 세션의 `comp` 유무 전수 확인 → 서버 종료 확인. 포트 7777 상시 모니터는 건드리지 않았다.
  - `git diff v0.1.7..HEAD -- index.html`으로 `.top` 그리드 · 미디어쿼리 · 공유 클래스(`.card`·`.big`·`.sub`·`.bar`) 불변 확인.
  - `index.html`의 `ctxBarSegs`·`ctxLegend`·`ctxNote`·`drawChart`·`renderMain`·`tick`·`charCard`·`SYS_SRC`를 Design §1.1의 D-1~D-23, §2.2 코드 블록·요소 목록과 한 항목씩 대조.
  - `README.md` 6 · 150 · 154행 문장 확인.
  - 삭제 대상(`compBar`·`.stack`·`.legend .li em`·`col`·`.cost-stats`·`.cost-left`·`.cost-right`·`.today-total`) grep 전수 확인.
- **Do 기록을 신뢰한 것** — 육안 SC 전부(SC-1~SC-7, SC-9~SC-14, SC-17~SC-22의 "보인다/읽힌다" 부분). 이 프로젝트는 CI도 스냅샷 테스트도 없고(RULE.md 예외 2), 렌더된 화면을 감사 세션이 다시 볼 수단이 없다. 대신 그 육안 판단을 떠받치는 코드 사실(색의 단일 출처, 층 개수, path 한 번, 스코프된 CSS)을 위와 같이 전부 코드로 확인해 육안 기록과 어긋나는 것이 없음을 봤다.
- **감사 범위 밖**
  - SC-12의 실물 확인 — `comp` 없는 세션이 아카이브·실사용 어디에도 없다.
  - 죽은 `heroCard`·`scoreCard`·`table.tools`·`.tb`·`.stats` — D-10이 범위 밖으로 둔 것(I-3).
  - B-4에서 만들었다 폐기한 캔버스 — do §5가 산출물이 아니라고 적었고 저장소 파일도 아니다.

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-06 | 감사 | Do 전 배치 검증 완료 |
