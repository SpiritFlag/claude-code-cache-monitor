# v0.1.9-enhance-browser-tab Do

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-browser-tab | v0.1.9 | 진행 중 | 2026-09-06 | 2026-09-06 |

---

## 1. 배치 현황

| ID | 모듈 | 하는 일 | 상태 | 세션 |
|---|---|---|---|---|
| B-1 | tab-core | `<link id="favicon">` 추가, `tabSession` · `tabTitle`(꼬리 없이 대사만) · `favImage` · `applyFavicon`(배지 인자는 받되 항상 false) · `updateTab`, `tick()` 첫 줄과 `es.onmessage` 끝의 호출 | 구현 완료 | 1 |
| B-2 | tab-core | `tabTitle`에 TTL 꼬리 ` · mm:ss` 추가 | 구현 완료 | 1 |
| B-3 | tab-attention | `prevChar` · `detectTabTransitions` · `startBlink` · `stopBlink` · `BLINK_MARK`, `focus` · `visibilitychange` 리스너, 두 곳의 `phase ^= 1` | 미착수 | |
| B-4 | tab-attention | `BADGE_STATES`, `applyFavicon`의 배지 그리기, `updateTab`에서 배지 여부 전달 | 미착수 | |
| B-5 | tab-notify | `#notifybtn` 마크업과 CSS, `NOTIFY_UI` · `applyNotifyBtn` · 클릭 핸들러 · `notifyAsk`, `detectTabTransitions`의 알림 분기 | 미착수 | |
| B-6 | tab-notify | README 요약 한 줄과 `## 브라우저 탭` 절, 전체 회귀 | 미착수 | |

## 2. 구현 결정

| # | 무엇 | 어떻게 | 이유 | 관련 |
|---|---|---|---|---|

## 3. 검증 기록

| 배치 | 수단 | 실행·확인 내용 | 결과 | 날짜 |
|---|---|---|---|---|
| B-1 · B-2 | 자동 테스트 | `node --test` (server.js 미변경 회귀 확인용) | 27 passed / 0 failed | 2026-09-06 |
| B-1 · B-2 | 사용자 육안 | `node server.js --dir /home/singi/workspace --port 7801`. 탭 아이콘이 맨 위 세션 캐릭터, 제목이 그 대사(SC-4), 다른 세션 클릭해도 탭 그대로(SC-1), 16px에서 working/idle 구분(SC-2), 몇 초 안에 아이콘 갱신(SC-3), 빈 폴더에서 `cc-monitor`/idle/콘솔 오류 없음(SC-13), `/favicon.ico` 404 없음(SC-15), TTL 꼬리가 1초마다 줄고 카드 숫자와 일치(SC-5) | 미확인 — 사용자 대기 중 | |

## 4. 질문 이력

| ID | 요지 | 결론 | 반영 위치 |
|---|---|---|---|

## 5. 진행 로그

### 세션 1 (2026-09-06)

- 시작: B-1~B-6 전체(모듈 tab-core · tab-attention · tab-notify). design.md·RULE.md·index.html 전체를 읽었다.
- 종료(중간): 사용자 요청으로 tab-core(B-1·B-2)까지만 구현하고 대기. `<link id="favicon">`, `tabSession`·`tabTitle`(TTL 꼬리 포함)·`favImage`·`applyFavicon`(badge 인자는 받되 늘 false)·`updateTab`을 index.html 끝에 추가하고 `tick()` 첫 줄·`es.onmessage` 끝에서 호출한다. `node --test` 27건 통과(회귀 없음). 포트 7801(`--dir /home/singi/workspace`)로 띄워둔 채 사용자 육안 확인 대기 중 — 확인 오면 §3 갱신 후 §1 상태를 검증 완료로 올리고 B-3(tab-attention)로 이어간다. 서버는 아직 종료 안 함(확인 뒤 종료 예정).

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-06 | 초안, 배치 현황 등재 | Design 확정 |
