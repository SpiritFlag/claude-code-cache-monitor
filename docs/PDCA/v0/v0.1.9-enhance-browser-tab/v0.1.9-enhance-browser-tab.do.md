# v0.1.9-enhance-browser-tab Do

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-browser-tab | v0.1.9 | 진행 중 | 2026-09-06 | 2026-09-06 |

---

## 1. 배치 현황

| ID | 모듈 | 하는 일 | 상태 | 세션 |
|---|---|---|---|---|
| B-1 | tab-core | `<link id="favicon">` 추가, `tabSession` · `tabTitle`(꼬리 없이 대사만) · `favImage` · `applyFavicon`(배지 인자는 받되 항상 false) · `updateTab`, `tick()` 첫 줄과 `es.onmessage` 끝의 호출 | 검증 완료 | 1 |
| B-2 | tab-core | `tabTitle`에 TTL 꼬리 ` · mm:ss` 추가 | 검증 완료 | 1 |
| B-3 | tab-attention | `prevChar` · `detectTabTransitions` · `startBlink` · `stopBlink` · `BLINK_MARK`, `focus` · `visibilitychange` 리스너, 두 곳의 `phase ^= 1` | 미착수 | |
| B-4 | tab-attention | `BADGE_STATES`, `applyFavicon`의 배지 그리기, `updateTab`에서 배지 여부 전달 | 미착수 | |
| B-5 | tab-notify | `#notifybtn` 마크업과 CSS, `NOTIFY_UI` · `applyNotifyBtn` · 클릭 핸들러 · `notifyAsk`, `detectTabTransitions`의 알림 분기 | 미착수 | |
| B-6 | tab-notify | README 요약 한 줄과 `## 브라우저 탭` 절, 전체 회귀 | 미착수 | |

## 2. 구현 결정

| # | 무엇 | 어떻게 | 이유 | 관련 |
|---|---|---|---|---|
| 1 | B-1의 `updateTab`이 `tabBlink`를 볼지 | 안 본다 — `document.title = tabTitle(s, now)`만 하고 끝낸다 | `tabBlink`·`BLINK_MARK`는 B-3 소관이라 아직 선언되지 않는다. B-1 시점엔 깜빡임 기능 자체가 없으므로 항상 대사만 보이는 게 맞다. B-3에서 `if (tabBlink && tabBlink.phase) title = BLINK_MARK + title` 형태로 얹는다 | B-1, B-3, D-6 · D-7 |

## 3. 검증 기록

| 배치 | 수단 | 실행·확인 내용 | 결과 | 날짜 |
|---|---|---|---|---|
| B-1 · B-2 | 자동 테스트 | `node --test` (server.js 미변경 회귀 확인용) | 27 passed / 0 failed | 2026-09-06 |
| B-1 · B-2 | 사용자 육안 | 포트 7801, `--dir /home/singi/workspace`(뒤에 `--dir /home/singi/.claude/projects/-home-singi-workspace-claude-code-cache-monitor`로 재기동해 이 대화 자체를 맨 위 세션으로 확인). 탭 아이콘이 맨 위 세션 캐릭터, 제목이 그 대사(SC-4) — 확인. 다른 세션 클릭해도 탭 그대로(SC-1) — 확인. `/favicon.ico` 404 없음(SC-15) — 확인. 16px에서 working/idle 구분되고 몇 초 안에 갱신(SC-2 · SC-3) — 확인(캐릭터 이미지 자체는 나중에 더 다듬을 여지 있다는 코멘트, §5 참고) | 확인 (2026-09-06) | 2026-09-06 |
| B-1 · B-2 | 사용자 육안 | 같은 서버(포트 7801). 제목 뒤 `mm:ss`가 1초마다 줄고 Cache TTL 카드 숫자와 일치(SC-5) | 확인 (2026-09-06) | 2026-09-06 |
| B-1 | 사용자 육안 | 포트 7802, 빈 디렉터리(`scratchpad/empty-proj-test`)로 재기동. 제목 `cc-monitor` · 아이콘 idle · 콘솔 오류 없음(SC-13) | 확인 (2026-09-06) | 2026-09-06 |
| B-1 · B-2 | 로그 · 수치 | 두 서버(7801 · 7802) 기동 로그에 `/favicon.ico` 404 요청 없음. 확인 뒤 두 서버 모두 종료(포트 7777 상시 모니터는 건드리지 않음) | 종료 확인 | 2026-09-06 |

## 4. 질문 이력

| ID | 요지 | 결론 | 반영 위치 |
|---|---|---|---|

## 5. 진행 로그

### 세션 1 (2026-09-06)

- 시작: 범위 식별 — `--scope` 인자 없음, do §1 첫 미착수 배치(B-1)의 모듈 `tab-core`(B-1·B-2)가 이번 세션 범위. design.md·RULE.md·index.html 전체를 읽었다.
- `<link id="favicon">`, `tabSession`·`tabTitle`(TTL 꼬리 포함)·`favImage`·`applyFavicon`(badge 인자는 받되 늘 false)·`updateTab`을 index.html 끝에 추가하고 `tick()` 첫 줄·`es.onmessage` 끝에서 호출했다. `node --test` 27건 통과(server.js 미변경, 회귀 없음).
- 사용자 육안 검증 SC-1·SC-2·SC-3·SC-4·SC-5·SC-13·SC-15 전부 확인(§3). 도중 사용자가 "이미지는 나중에 바꾸긴 해야할듯"이라고 코멘트 — SC-2는 합격이라 이번 배치는 막히지 않지만, `FAV_CROP`/캐릭터 이미지 자체를 16px에서 더 또렷하게 다듬는 건 이 사이클 범위 밖 개선 후보로 남긴다(close가 report §3.4로 보낼 것).
- 종료: tab-core(B-1·B-2) 검증 완료. 남은 배치는 B-3~B-6(모듈 tab-attention · tab-notify). 다음 세션 `--scope tab-attention`(B-3·B-4)부터. 다음 세션이 볼 것: `detectTabTransitions`는 `es.onmessage`에서 `state = snap` **이전**에 불러야 하고(D-3·D-4), `updateTab()`은 지금 blink를 전혀 안 보므로 B-3에서 `tabBlink` 검사를 추가해야 한다(§2 구현 결정 예정).

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-06 | 초안, 배치 현황 등재 | Design 확정 |
| 0.2 | 2026-09-06 | 세션 1, B-1·B-2 검증 완료 | tab-core 구현 · 사용자 육안 확인 |
