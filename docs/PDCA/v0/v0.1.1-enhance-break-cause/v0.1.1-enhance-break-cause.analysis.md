# v0.1.1-enhance-break-cause Analysis

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-break-cause | v0.1.1 | 완료 | 2026-09-05 | 2026-09-05 |

---

## 1. SC 판정

| SC | 기준 | do 근거 | 독립 재확인 | 판정 |
|---|---|---|---|---|
| SC-1 | prev.in=5,371인 픽스처에서 보정 후 rewrite가 0이 되어 브레이크가 기록되지 않는다. | Do §3 B-1 자동 테스트 | `node --test` 재실행(7 passed, 0 failed). `test/fixtures/prev-in-tail.jsonl` 첫 호출의 `input_tokens`가 5371임을 직접 읽었고, `replay`로 돌린 결과 `breaks.length === 0` | 실증 |
| SC-2 | 한 달치 아카이브를 `--dir`로 띄웠을 때 `/api/state`의 폴더 전체 byCause에서 `unexplained.n`이 17에서 7이 된다. | 없음 (Do §3 B-3이 "미검증(보류)"로 기록) | 불가 — 대상 폴더 `data/fishing/extract`가 이 머신에 없다 | 미실증 — 한 달치 아카이브 미도착(2026-09-07 제공 예정). 사용자 판단으로 이월 → report §3.1 |
| SC-3 | 배열형 마커 픽스처에서 직후 브레이크가 `session_resume`이고, `last-prompt`·`file-history-snapshot`만 있는 구간의 브레이크는 `session_resume`이 아니다. | Do §3 B-3 자동 테스트 | `test/resume-cause.test.js`를 읽어 두 단언이 실제로 이 SC를 덮는 것을 확인한 뒤 `node --test` 재실행. 더해 `replay`로 직접 돌려 `resume-array-marker`의 cause가 `session_resume`, `resume-noise`의 cause가 `unexplained`임을 확인 | 실증 |
| SC-4 | 한 달치에서 `c69ed6ac` 08-24 04:58 UTC 건이 `session_resume`이고, `258192f3` 08-26 05:18 UTC 건은 `compact`로 남는다. | 없음 (Do §3 B-3 "미검증(보류)") | 불가 — 같은 사유 | 미실증 — 한 달치 아카이브 미도착. 사용자 판단으로 이월 → report §3.1 |
| SC-5 | 사용자 PC의 세션 128ae4a7 01:41 UTC 건이 `session_resume`으로 분류된다. | 없음 | 불가 — 원본 jsonl이 사용자 회사 PC에 있다(Plan R-1) | 미실증 — 원본 미확보. 사용자 판단으로 이월 → report §3.1 |
| SC-6 | 128ae4a7 수치(직전 cr 510,833 → cw 4,348 · cr 506,948)를 담은 픽스처에서 `breakpoint_shift`가 나오고 breakCost·streak가 변하지 않는다. | Do §3 B-4 자동 테스트 | `test/shift-cause.test.js`가 cause·`breakCost`·`streak` 셋을 모두 단언함을 읽고 확인한 뒤 재실행. 더해 `replay`로 직접 돌려 cause=`breakpoint_shift` · `breakCost === 0` · `streak === 2` 확인 | 실증 |
| SC-7 | 한 달치 변경 전후 byCause 차이가 F-2의 9건 소멸, F-5의 3건 이동, F-8의 effort_change 5건 이동뿐이다. | 없음 (Do §3 B-4 "미검증(보류)") | 불가 — 같은 사유 | 미실증 — 한 달치 아카이브 미도착. 사용자 판단으로 이월 → report §3.1 |
| SC-8 | `/api/detail`의 breaks 항목마다 FR-6 필드 13개가 있고 `prefixIntact`가 `curCr >= prevCr`과 일치한다. | Do §3 B-1 자동 테스트 | `test/break-core.test.js`는 메모리 레코드만 단언해 `/api/detail`을 거치지 않는다. 그래서 직접 `detail(id)`를 불러 JSON 직렬화한 키를 세었다 — 픽스처 4개 전부에서 `prevIn·prevCr·prevTotal·curIn·curCw·curCr·cw1h·cw5m·shrink·grew·gapMin·prefixIntact·events` 13필드가 모두 나오고, `server.js:334`가 `s.breaks`를 그대로 실어 보내 필드가 깎이지 않음을 코드로 확인 | 실증 |
| SC-9 | 검증 스크립트를 한 달치에 돌린 원인별 건수가 같은 폴더의 `/api/state` byCause 합과 같다. | Do §3 B-2 로그·수치 (대상을 `test/fixtures`로 바꿔 방법만 확인) | `node scripts/diag-breaks.js --dir test/fixtures --top 5`와 같은 폴더를 `replay`한 snapshot의 byCause 합을 대조 — `unexplained n=2 rewrite=70002`, `session_resume n=2 rewrite=60000`, `breakpoint_shift n=1 rewrite=3885`로 건수·rewrite·extra까지 일치. `diag-shrink.js`도 브레이크 4건마다 사이 레코드를 찍는 것을 확인 | 부분 실증 — 스크립트가 서버와 같은 판정을 낸다는 것은 확인됐으나, SC가 지정한 대상인 한 달치 아카이브(83파일)가 빠지고 픽스처 5파일로만 확인했다 |
| SC-10 | `node --test test/`가 통과하고, `test/fixtures/` 안에 `lastPrompt`·`cwd`·홈 경로 문자열이 없다. | Do §3 B-1·B-5 자동 테스트 | `node --test` 재실행 7 passed 0 failed. `grep -rn -e lastPrompt -e '"cwd"' -e /home/ -e Users test/fixtures/`가 매치 없음(exit 1) | 실증 — 다만 명령은 `node --test test/`가 아니라 인자 없는 `node --test`다(Do §2 #1). 같은 파일들을 discover하므로 기준 자체는 충족한다 |
| SC-11 | 한 달치를 띄운 화면의 습관 순위표에 `session_resume` 행이 3건으로 보이고, 제외 안내 줄에 compact와 breakpoint_shift 건수가 보인다. | Do §3 B-5 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 사용자가 본 화면은 `test/fixtures`를 띄운 것이고 한 달치가 아니다. "세션 재개" 행과 바뀐 제외 안내 줄이 화면에 나온다는 것까지가 확인 범위다 | 부분 실증 — 표시 자체는 사용자 육안으로 확인됐으나, SC가 요구한 "한 달치에서 3건"이라는 수치는 대상 폴더가 없어 빠졌다 |
| SC-12 | 화면의 캐시 깨짐 손실 금액이 `/api/state`의 breakCost와 같고, byCause의 compact·breakpoint_shift extra는 breakCost에 들어 있지 않다. | 없음 (Do §3 B-4 "미검증(보류)") | 뒷절은 실증했다 — `node server.js --dir test/fixtures --port 7791`로 띄운 `/api/state`에서 세션 `s-breakpoint-shift`가 `breakCost: 0`인데 `byCause.breakpoint_shift.extra: 0.0089355`다. 앞절은 `index.html`의 141·173·226행이 `s.breakCost`를 그대로 쓰는 것을 코드로 확인했을 뿐, 화면 수치 대조는 못 했다 | 부분 실증 — 서버 쪽 제외는 확인됐고, 화면 금액 대조가 빠졌다 |
| SC-13 | 한 달치에서 F-8의 5건이 `breakpoint_shift`로 분류되고, rewrite 2만 이상인 effort_change 136건은 그대로다. | 없음 (Do §3 B-4 "미검증(보류)") | 불가 — 같은 사유 | 미실증 — 한 달치 아카이브 미도착. 사용자 판단으로 이월 → report §3.1 |

## 2. 결정 이행

| D | 결정 | 지켰나 | 어긋남 |
|---|---|---|---|
| D-1 | `server.js`에 `require.main` 가드를 넣고 `reset`·`replay`를 export한다 | 예 | `server.js:370-396`. `node server.js --dir test/fixtures --port 7791` 기동 로그가 스캔·calibration·watch·listen까지 예전과 같이 나오는 것을 재확인했다 |
| D-2 | 픽스처는 손으로 쓴 합성 jsonl | 예 | 5개 전부 합성이고 개인정보가 없다(SC-10) |
| D-3 | `rewrite = min(max(0, (prev.total − prev.in) − t.cr), t.cw + t.in)` | 예 | `server.js:231-232` |
| D-4 | `shrink = prev.cr − t.cr`(음수 허용) | 예 | `server.js:236` |
| D-5 | `case 'user'`에서 `isMeta` + 문자열·배열 content를 `/continue from where you left off/i`로 검사 | 예 | `server.js:186-188`, `markerText`는 `server.js:77-81` |
| D-6 | `markerSeen:Set`으로 마커 uuid 중복 무시 | 예 | `server.js:187`, 초기화·truncate 리셋 양쪽에 추가됨(`server.js:282`·`284`) |
| D-7 | `model_switch → compact → ttl_expiry → session_resume → breakpoint_shift → effort_change → prefix:* → unexplained` | 예 | `server.js:240-247` 순서가 그대로다 |
| D-8 | `BP_MAX_REWRITE = 20000`, `BP_SHRINK_SLACK = 3000` | 예 | `server.js:28-29`, 잠정치 주석도 있다 |
| D-9 | `FREE_CAUSES`로 `breakCost`·`brokeNow`를 막고 `byCause`에는 그대로 쌓는다 | 예 | `server.js:248`·`251`, `byCause` 누적은 무조건이라 화면이 건수·금액을 볼 수 있다 |
| D-10 | 검증 스크립트를 `scripts/`에 둔다 | 예 | `scripts/diag-breaks.js`·`scripts/diag-shrink.js`가 추적된다 |
| D-11 | 한 달치 아카이브를 `data/fishing/extract/`에 풀고 SC-2·4·7·9·13을 거기서 확인한다 | 아니오 | 아카이브가 도착하지 않아 이 경로가 한 번도 쓰이지 않았다. 결정 자체가 뒤집힌 것은 아니고 이행되지 않은 것이다. 위 다섯 SC의 미실증·부분 실증이 전부 여기서 나온다 |
| D-12 | `events`에 소비된 `f.pending` 배열을 그대로 담는다 | 예 | `server.js:229`에서 배열을 넘긴 뒤 `f.pending`을 새 배열로 갈아 끼워, 저장된 참조가 뒤에 덮이지 않는다. `diag-shrink` 출력에서 `events=["resume"]`을 확인했다 |

## 3. 결함

| ID | 발견 | 위치 | 처리 | 재검증 |
|---|---|---|---|---|
| I-1 | 세션 카드의 "캐시 깨짐 손실 $X (N건)" 줄에서 금액과 건수가 서로 다른 집합을 센다. 이번 변경으로 금액(`s.breakCost`)은 compact·breakpoint_shift를 빼게 됐는데 건수(`d.breaks.length`)는 그 둘을 그대로 포함한다. 변경 전에는 금액도 compact를 포함해 둘이 맞았으므로, 이 어긋남은 이 사이클이 만든 것이다 | `index.html:226` | 이월 → report §3.1 | — (Plan §2.2·R-6이 `index.html` 변경을 §2.1의 두 곳으로 못 박았고 226행은 그 둘이 아니다. 범위 안에서 고칠 수 없다) |
| I-2 | 같은 줄의 건수는 `/api/detail`이 돌려주는 최근 40건이라, 세션의 브레이크가 40건을 넘으면 실제보다 적게 보인다. 이 사이클 이전부터 있던 것이다 | `server.js:334`, `index.html:226` | 이월 → report §3.4 | — |
| I-3 | RULE.md가 통합 브랜치를 `develop`으로 적었으나 저장소에 그 브랜치가 없다. 이 사이클은 `main`(23c9c03)에서 갈라져 있었다 | `docs/RULE.md:17` | 이 세션에서 `main` 위에 `develop`을 신설해 해소(사용자 결정) → report §6 | close §8에서 `git branch -a`로 확인한다 |

## 4. 감사 방법

- 직접 실행한 것
  - `node --test` — 7 passed, 0 failed. 그 전에 `test/break-core.test.js`·`test/resume-cause.test.js`·`test/shift-cause.test.js`를 열어 각 단언이 해당 SC를 실제로 덮는지 읽었다.
  - `replay`로 픽스처 5개를 직접 돌려 브레이크 수·cause·`breakCost`·`streak`을 확인하고, `detail(id)`를 JSON으로 직렬화해 진단 13필드가 살아 나오는지 세었다.
  - `node scripts/diag-breaks.js --dir test/fixtures --top 5`와 같은 폴더의 snapshot byCause 집계를 대조했다. `node scripts/diag-shrink.js --dir test/fixtures --min 2000`을 돌려 출력 형태를 확인했다.
  - `node server.js --dir test/fixtures --port 7791`로 기동 로그를 보고 `/api/state`를 조회했다.
  - `grep -rn -e lastPrompt -e '"cwd"' -e /home/ -e Users test/fixtures/`.
  - `git diff origin/main..HEAD`로 `server.js`·`index.html` 변경을 D-1~D-12에 하나씩 대조했다.
- Do 기록을 신뢰한 것
  - SC-11의 사용자 육안(2026-09-05, `test/fixtures` 화면에서 "세션 재개" 행과 바뀐 제외 안내 줄 확인). 사용자 브라우저에서 일어난 일이라 이 세션이 재현할 수 없다.
- 감사 범위 밖
  - 한 달치 아카이브(`data/fishing/extract`)를 대상으로 하는 SC-2·SC-4·SC-7·SC-13과, 사용자 회사 PC의 세션 128ae4a7을 대상으로 하는 SC-5. 아카이브는 2026-09-07 제공 예정이고 이 머신에 없다. 사용자가 2026-09-05에 이 다섯을 미실증인 채로 사이클을 닫기로 결정했다. SC-9·SC-11·SC-12의 빠진 절반도 같은 대상에 걸려 있다.

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-05 | 감사 | Do 배치 구현 완료, 아카이브 대상 검증은 사용자 판단으로 미실증 확정 |
