# v0.1.1-enhance-break-cause Do

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-break-cause | v0.1.1 | 진행 중 | 2026-09-05 | 2026-09-05 |

---

## 1. 배치 현황

> 상태: 미착수 / 진행 중 / 구현 완료 / 검증 완료. close는 전건 `검증 완료`일 때만 시작한다.

| ID | 모듈 | 하는 일 | 상태 | 세션 |
|---|---|---|---|---|
| B-1 | `break-core` | prev 객체에 `in`·`cr` 저장, rewrite 보정, `shrink`·`grew`·`prefixIntact` 계산, breaks 레코드 13필드 추가, `reset`·`replay` export와 `require.main` 가드. `test/`·`test/fixtures/` 신설과 `helper.js`, 픽스처 `prev-in-tail`·`breakpoint-shift` 작성 | 검증 완료 | 1 |
| B-2 | `break-core` | 검증 스크립트 둘 | 구현 완료 | 1 |
| B-3 | `resume-cause` | `RESUME_RE` 마커 검출, `markerSeen` uuid 중복 무시, 체인에 `session_resume` 삽입. 픽스처 셋 추가 | 구현 완료 | 1 |
| B-4 | `shift-cause` | `BP_*` 상수와 `breakpoint_shift` 분류, `FREE_CAUSES`로 `breakCost`·`brokeNow` 제외, `index.html`의 차감 두 줄 제거 | 미착수 | |
| B-5 | `habits-ui` | `HABITS`에 `session_resume` 행, 제외 안내 줄에 `breakpoint_shift` 합산 | 미착수 | |

## 2. 구현 결정

> Design이 정하지 않은 세부를 어떻게 했는지. 설계를 바꾸는 것이면 여기 쓰지 말고 질문 파일로.
> Design에 없는 테스트를 추가했으면 여기 이유를 적는다. Design이 지목하지 않은 기존 테스트를 고치는 건 여기 적을 수 없다(질문 파일). 불안정한 테스트를 발견하면 끄지 말고 여기 기록한다.

| # | 무엇 | 어떻게 | 이유 | 관련 |
|---|---|---|---|---|
| 1 | RULE.md의 `node --test test/`가 이 머신의 Node v24에서 디렉터리 인자를 모듈로 require하려다 `MODULE_NOT_FOUND`로 실패함(`node --test <파일>` 또는 인자 없는 `node --test`는 정상 동작하고 결과가 동일) | do §3 실행 로그에 인자 없는 `node --test`(test/ 하위 전체를 자동 탐색)로 기록한다 | RULE.md·design 어느 쪽도 바꾸지 않고 같은 파일들을 discover하는 동등한 호출로 대체. 검증 기준(통과/실패)에는 차이가 없다 | B-1 |

## 3. 검증 기록

> 배치마다. 자동 테스트는 명령과 결과, 사용자 육안은 날짜와 확인 내용(사용자가 확인했다는 사실을 남긴다), 로그·수치는 실측값.

| 배치 | 수단 | 실행·확인 내용 | 결과 | 날짜 |
|---|---|---|---|---|
| B-1 | 자동 테스트 | `node --test`(test/ 자동 탐색, §2 #1 참고) | 3 passed, 0 failed (`prev-in-tail` breaks.length===0 · `breakpoint-shift` 13필드·rewrite/shrink=3885) | 2026-09-05 |
| B-1 | 로그·수치 | `node server.js --dir test/fixtures --port 7788` 기동 로그 확인 — `require.main` 가드 이후에도 스캔·watch·listen이 예전과 동일하게 도는지 | `[cc-monitor] scanning …` → `sessions: 2 files: 2` → `watching (recursive)` → `http://localhost:7788` 정상 출력, 크래시 없음 | 2026-09-05 |
| B-2 | 로그·수치 | `node scripts/diag-breaks.js --dir test/fixtures --top 5`의 원인별 집계와, 같은 폴더로 띄운 `curl localhost:7788/api/state`의 세션별 `byCause` 대조. `data/fishing/extract`(D-11)가 이 머신에 없어 실제 대상 대신 `test/fixtures`로 방법을 검증 — SC-9 자체는 미검증(§5 참고) | 둘 다 `unexplained n=1 rewrite=3885 extra=$0.0089355`로 일치 | 2026-09-05 |
| B-2 | 로그·수치 | `node scripts/diag-shrink.js --dir test/fixtures --min 2000` — 브레이크마다 사이 레코드를 찍는지 | `breakpoint-shift` 브레이크 구간에 `assistant uuid=a2` 1건 출력, 판정(cause 등) 없이 원본 필드만 덤프함을 확인 | 2026-09-05 |
| B-3 | 자동 테스트 | `node --test`(§2 #1) | 6 passed, 0 failed — `resume-array-marker`(cause===session_resume, rewrite===30000) · `resume-noise`(SC-3, cause≠session_resume) · `resume-dup-uuid`(FR-3, 두 번째 브레이크는 session_resume 아님) | 2026-09-05 |
| B-3 | 로그·수치 | SC-2(`unexplained.n===7`)·SC-4(c69ed6ac·258192f3 두 건의 cause)는 `data/fishing/extract`(D-11) 대상이라 이 머신에서 미실행 — §5 참고 | 미검증(보류) | — |

## 4. 질문 이력

> 질문 파일은 답을 반영한 뒤 삭제한다. 여기엔 번호·요지·결론·반영 위치만 남긴다.

| ID | 요지 | 결론 | 반영 위치 |
|---|---|---|---|

## 5. 진행 로그

> 세션마다 시작 · 종료 시 한 단락. 막힌 것, 다음 세션이 먼저 볼 것.

### 세션 1 (2026-09-05)

- 시작: B-1·B-2(모듈 `break-core`) 담당. design.md와 RULE.md를 읽음. server.js·index.html 현재 구조를 확인함(216~235행 판정부, 260~262행 readFile 초기화, 348~355행 기동 블록).
- 계속: 사용자가 "아카이브는 월요일(2026-09-07)에 온다, 개발은 미리 해야 한다"고 확인 — design R-1이 이미 예견한 상황이므로 질문 파일 없이 B-3(`resume-cause`)을 합성 픽스처로 마저 구현하고, 아카이브가 필요한 검증만 보류로 넘김.
- B-3 구현: `RESUME_RE`·`markerText` 추가, `case 'user'`에 마커 검출(`isMeta` + 정규식 + `markerSeen` uuid 중복 무시), 판정 체인에 `ttl_expiry` 다음·`effort_change` 이전 자리에 `session_resume` 삽입(D-7 순서), `readFile`의 두 초기화 지점(신규 생성·truncate 리셋)에 `markerSeen: new Set()` 추가. 픽스처 3종(`resume-array-marker`·`resume-noise`·`resume-dup-uuid`)과 `test/resume-cause.test.js` 작성, SC-3·FR-3 자동 테스트 통과.
- 종료: B-1 검증 완료. B-2·B-3은 구현 완료 — 로직·자동 테스트는 통과했으나 SC-9(B-2)·SC-2·SC-4(B-3)가 요구하는 실제 대상 `data/fishing/extract`(D-11) 아카이브가 이 머신에 없어 그 폴더 기준 최종 확인은 못 함. 다음 세션 주의점: (1) 2026-09-07 아카이브가 들어오면 `node server.js --dir data/fishing/extract --port 7788`을 띄우고 `node scripts/diag-breaks.js --dir data/fishing/extract`·`curl /api/state`·`/api/detail`을 대조해 B-2·B-3을 검증 완료로 올릴 것. 이때 SC-5(128ae4a7 원본, 사용자 육안)도 함께 확인할 것. (2) `node --test test/`는 이 머신 Node v24에서 디렉터리 인자를 못 받는다(§2 #1) — 인자 없는 `node --test`를 계속 쓸 것. (3) 다음 배치는 B-4(`shift-cause`) — `resume-cause`가 판정 체인에 끼어들었으니 B-4 구현 전에 216행대 판정부를 다시 확인할 것.

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-05 | 초안, 배치 현황 등재 | Design 확정 |
| 0.2 | 2026-09-05 | 세션 1: B-1 검증 완료, B-2·B-3 구현 완료(아카이브 대상 검증 보류) | 배치 B-1~B-3 |
