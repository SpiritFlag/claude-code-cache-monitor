# v0.1.5-enhance-fuel-gauge Analysis

| 프로젝트 | 사이클 | 버전 | 상태 | 작성 | 갱신 |
|---|---|---|---|---|---|
| claude-code-cache-monitor | enhance-fuel-gauge | v0.1.5 | 완료 | 2026-09-05 | 2026-09-05 |

---

## 1. SC 판정

| SC | 기준 | do 근거 | 독립 재확인 | 판정 |
|---|---|---|---|---|
| SC-1 | 컨텍스트 카드에 "X로 올리면" 칸이 없다 | Do §3 B-1 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 `escalationAdvice` · `switchCells` 심볼이 server.js · index.html 어디에도 없음을 grep으로 확인 | 실증 |
| SC-2 | 같은 카드에 새 세션 원칙 문구가 보인다 | Do §3 B-1 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 index.html:327의 렌더 문구가 D-3 확정 표현과 글자 단위로 같음을 확인 | 실증 |
| SC-3 | `/api/state` 세션 객체에 `switch` 키가 없다 | Do §3 B-1 로그·수치 | `node server.js --dir data/fishing --port 23310` 기동 후 `/api/state` 세션 키 목록 출력 — `switch` 없음 | 실증 |
| SC-4 | `/api/state` 세션 객체에 `modelCompare` 키가 없다 | Do §3 B-1 로그·수치 | 같은 응답에서 `modelCompare` 없음. `mainModel`도 없음 | 실증 |
| SC-5 | Cost 카드에 모델 비교 4칸이 없다 | Do §3 B-1 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 `modelCard` 렌더 함수 부재 확인 | 실증 |
| SC-6 | 혼용 세션 Cost 카드에 "혼용 세션 실비" 줄이 없다 | Do §3 B-1 사용자 육안 2026-09-05(세션 `3c8d6f02`) | 재확인 불가(사용자 확인 신뢰) | 실증 |
| SC-7 | 합성 픽스처 활성시간이 손계산 값과 같다 | Do §3 B-2 자동 테스트 | `test/active-time.test.js`를 읽어 `assert.equal(s.activeMs, 15000 + 10000)`으로 그룹별 손계산 합을 직접 단언함을 확인. `node --test` 재실행 통과 | 실증 |
| SC-8 | 타임스탬프 없는 줄을 끼워도 활성시간이 변하지 않는다 | Do §3 B-2 자동 테스트 | 같은 파일에서 `active-notime`과 `active-basic`의 `activeMs`를 동등 비교함을 확인. 재실행 통과 | 실증 |
| SC-9 | 서브에이전트 파일을 붙여도 활성시간이 변하지 않는다 | Do §3 B-2 자동 테스트 | `active-sidechain`과 `active-basic`의 동등 비교 확인. 재실행 통과 | 실증 |
| SC-10 | KST 04:59 그룹과 05:00 그룹이 다른 날짜 키에 들어간다 | Do §3 B-2 자동 테스트 | 테스트가 `days['2026-09-01'].activeMs === 60000` · `days['2026-09-02'].activeMs === 30000`을 각각 단언함을 확인. 재실행 통과 | 실증 |
| SC-11 | `data/fishing` 폴더 활성시간이 고정 수치이고 plan 참고값 43.82시간과 ±1% 안 | Do §3 B-2 자동 테스트 | `test/archive-fuel.test.js`가 `157774338`을 ±1%로 단언함을 확인. 재실행 시 skip 아님(skipped 0). 서버 실측도 `folder.activeMs=157774338`(43.83시간)으로 같고 참고값 43.82시간과의 차는 1% 안 | 실증 |
| SC-12 | 2026-08-13 키의 활성시간 · 비용이 고정 수치이고 참고값 4.20시간 · $195.66과 ±1% 안 | Do §3 B-2 자동 테스트 | 테스트가 `15123657` · `195.66`을 ±1%로 단언. 서버 실측 `{activeMs:15123657, cost:195.657…}`으로 같음 | 실증 |
| SC-13 | Cost 카드에 오늘 활성시간이 보인다 | Do §3 B-3 사용자 육안 2026-09-05(4차 확인) | 재확인 불가(사용자 확인 신뢰). 보조로 `fuelCells`의 "오늘" 행이 `활성 ${hrs(ms)}`를 렌더함을 확인 | 실증 |
| SC-14 | Cost 카드에 오늘 누적 비용이 보인다 | Do §3 B-3 사용자 육안 2026-09-05(1차 3칸 그리드 시점) | Cost 카드 전문을 읽어 확인 — 카드 상단 큰 숫자는 선택 세션의 총액이고, 연비 위젯의 "오늘" 행은 $/h와 활성시간만 적는다. 오늘 누적 비용(총액 $)을 적는 자리가 화면 어디에도 없다. D-2가 do B-3에서 $/h 중심 위젯으로 재설계되며 이 칸이 사라졌다 | 범위 변경(→ report §3.3) |
| SC-15 | Cost 카드에 오늘 활성시간당 비용이 보인다 | Do §3 B-3 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 "오늘" 행의 `perHrNum(today.cost, today.activeMs)` 렌더 확인 | 실증 |
| SC-16 | 오늘 값 옆에 폴더 전체 활성시간당 비용이 나란히 보인다 | Do §3 B-3 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). "나란히"는 D-2 재수정으로 가로 3칸이 아니라 같은 척도의 세로 3줄이 됐다 | 실증 |
| SC-17 | 실사용 폴더에서 KST 01~02시 세션 비용이 전날 합계에 들어 있다 | Do §3 B-2 로그·수치 | `node server.js --dir ~/.claude/projects --port 23311` 기동 후 세션별 KST 시각과 day key 대조 — KST 2026-09-05 01:19 · 01:34 · 02:05에 시작한 세션 셋($4.49 · $3.92 · $6.67)이 모두 `dayKey=2026-09-04`이고, `folder.days['2026-09-04'].cost`가 정확히 $15.08 | 실증 |
| SC-18 | 아바타 아래에 모델명 말풍선이 보인다 | Do §3 B-3 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 index.html:182의 `.bubble.down` 한 줄 확인 | 실증 |
| SC-19 | 세션을 바꾸면 말풍선 모델명이 그 세션 모델로 바뀐다 | Do §3 B-3 사용자 육안 2026-09-05(opus 세션 `301f5472`에서 "opus-5") | 재확인 불가(사용자 확인 신뢰). 보조로 말풍선이 `short(s.model)`로 선택 세션에서 파생됨을 확인 | 실증 |
| SC-20 | 컨텍스트 카드에 "지금 사고 나면" 줄이 없다 | Do §3 B-1 사용자 육안 2026-09-05 | index.html 전문에 "지금 사고 나면" 문자열 없음 | 실증 |
| SC-21 | 살아 있는 세션 TTL 카드에 "깨뜨리면 $X"가 그대로 보인다 | Do §3 B-1 사용자 육안 2026-09-05 | index.html:426의 `깨뜨리면 <b>${usd(s.riskUsd)}</b>` 렌더가 그대로 남아 있음 | 실증 |
| SC-22 | `model-compare.test.js`를 뺀 기존 테스트 16건이 계속 통과한다 | Do §3 B-1 자동 테스트(16 passed) | `node --test` 재실행 21 pass · 0 fail. 이번 사이클 신설 5건(active 4 · archive-fuel 1)을 빼면 기존 16건이고 실패 없음. `test/model-compare.test.js`는 삭제됨 | 실증 |
| SC-23 | README "숫자 읽을 때"에 모델 비교 문단과 사고 비용 문단이 없다 | 없음 — Do §3에 README 확인 기록이 없다(→ §3 I-2) | README.md 직접 확인 — "모델 비교" · "사고 나면" · "다른 모델이었다면" 문자열 없음 | 실증 |
| SC-24 | README "숫자 읽을 때"에 활성시간 정의와 05시 경계 문단이 있다 | 없음 — Do §3에 README 확인 기록이 없다(→ §3 I-2) | README.md:153에 활성시간 정의(requestId 그룹 기준, 벽시계 아님), :154에 KST 05시 경계 문단 확인 | 실증 |
| SC-25 | Cost 카드에 그 세션의 활성시간이 보인다 | Do §3 B-3 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 "이 세션" 행의 `활성 ${hrs(s.activeMs)}` 렌더 확인 | 실증 |
| SC-26 | Cost 카드에 그 세션의 활성시간당 비용이 보인다 | Do §3 B-3 사용자 육안 2026-09-05 | 재확인 불가(사용자 확인 신뢰). 보조로 "이 세션" 행의 `perHrNum(s.cost, s.activeMs)` 렌더 확인 | 실증 |
| SC-27 | 세션 `7014916b`의 활성시간이 do가 고정한 수치다 | Do §3 B-2 자동 테스트 | 테스트가 `sessions` 맵에서 `7014916b`로 시작하는 세션을 찾아 `5103156`을 ±1%로 단언함을 확인. 서버 실측 `activeMs=5103156`으로 같음 | 실증 |

## 2. 결정 이행

| D | 결정 | 지켰나 | 어긋남 |
|---|---|---|---|
| D-1 | "오늘"은 폴더 전체 집계, 날짜는 선택 세션이 속한 하루(do 재수정) | 예 | |
| D-2 | 연비 세 값을 가로트랙+원 3줄 위젯으로(do 재수정) | 예 | |
| D-3 | 원칙 문구 `모델은 세션 안에서 바꾸지 않습니다 — 바꾸면 컨텍스트 전체를 다시 씁니다`(do 재수정) | 예 | |
| D-4 | `mainModel` 삭제 · `modelCalls` 존치 | 예 | 스냅숏에 `mainModel` 없음, `s.modelCalls` 누적은 server.js:63 초기화 · :327 증가로 남아 있음 |
| D-5 | SC-11 · SC-12를 아카이브 조건부 테스트로 | 예 | `fs.existsSync`로 skip 처리. 이 머신에서는 실제로 실행됨(skipped 0) |
| D-6 | 활성시간 훅을 uuid 중복 제거 직후 · `switch` 진입 전에 건다 | 예 | server.js:263 `trackActive(f, d, ts)`가 uuid dedupe(:254) 뒤, `switch`(:265) 앞 |
| D-7 | 사이드체인을 `f.isSub \|\| d.isSidechain === true` 둘 다로 판정 | 예 | server.js:231 |
| D-8 | 그룹이 끝나는 자리에서 확정하고 `snapshot()` 진입 시 열린 그룹을 전부 확정 | 예 | `flushGroup`이 그룹 전환·비-assistant 이벤트에서 호출되고, snapshot 진입부(:426)에서 전 파일 flush |
| D-9 | 폴더 합계를 `sessions` 맵 전체에서 낸다 | 예 | server.js:245. 실측 `folder.cost=$2007.92`가 plan F-9 참고값과 일치 |
| D-10 | `ts + 4h`의 UTC 날짜를 키로, 서버와 화면이 같은 식을 각자 갖고, 화면은 `lastTs`로 오늘 키를 고른다 | 예 | server.js:211-212와 index.html:133-134가 같은 두 줄. `fuelCells`는 `dayKey(s.lastTs)` 사용 |
| D-11 | 활성시간이 벽시계가 아님을 레이블과 툴팁으로 드러낸다 | 예 | D-2 재수정으로 레이블 자리는 "활성 {시간}" 표기가 이어받았고, 툴팁에 "모델이 실제로 돌던 시간만 합산, 대기·생각 시간 제외"가 그대로 있다 |
| D-12 | `.bubble.down` 변형을 아바타 뒤에 한 줄 붙이고 꼬리는 제거 | 예 | index.html:27-28. `.bubble.down::after { display:none }`로 꼬리 없음 |
| D-13 | `modelCard` · `switchCells`로 죽는 CSS와 `.whatif` 계열을 지운다 | 예 | `.whatif` · `.wl` · `.wv` · `.wd` · `.whatif.sw` · `.modal tr.best` 전부 없음 |
| D-14 | 좁은 화면 규칙 주석의 근거를 연비로 고쳐 쓴다 | 아니오 | 주석은 `whatif`를 "연비"로 바꿨지만 형태를 "3-up 연비 grid"로 적었다. D-2가 do B-3에서 세로 3줄 트랙으로 재수정되며 이 서술이 현재 구현과 어긋났다(→ §3 I-1, 수정함) |

## 3. 결함

| ID | 발견 | 위치 | 처리 | 재검증 |
|---|---|---|---|---|
| I-1 | 좁은 화면 규칙(1280px 미만에서 Cost를 넓은 열로 보내는 규칙)의 근거 주석이 "3-up 연비 grid"라고 적혀 있다. D-2 재수정으로 실제 위젯은 세로 3줄 가로트랙이라 주석만 옛 형태를 설명한다. 다음에 이 레이아웃을 건드리는 사람이 존재하지 않는 3칸 그리드를 근거로 판단하게 된다 | `index.html:107` | 수정 — 근거를 "연비 행이 고정폭 값 옆에서 읽을 만한 트랙 길이를 유지하려면 넓은 열이 필요하다"로 고쳐 씀. 주석이라 회귀 테스트를 붙일 대상이 아니다 | `node --test` 21 pass · 0 fail. CSS 주석이라 렌더 영향 없음 |
| I-2 | SC-23 · SC-24는 검증 수단이 "사용자 육안"인데 do §3의 육안 행은 화면(Cost 카드 · 말풍선 · 컨텍스트 카드)만 적었고 README를 봤다는 기록이 없다. 두 SC를 덮는 do 근거가 비어 있었다 | `…do.md` §3 | 감사에서 README.md를 직접 읽어 실증으로 대체(§1 SC-23 · SC-24). do 문서를 소급 수정하지 않는다. 재발 방지는 회고로 → report §5 | 파일 내용 확인으로 실증됨 |

## 4. 감사 방법

- 직접 실행한 것: `node --test`(수정 전후 2회, 21 pass · 0 fail · skipped 0) / `node server.js --dir data/fishing --port 23310` + `/api/state`(세션 키 목록 · `folder.activeMs` · `days['2026-08-13']` · 세션 `activeMs`) / `node server.js --dir ~/.claude/projects --port 23311` + `/api/state`(세션별 KST 시각과 day key 대조) / `test/active-time.test.js` · `test/archive-fuel.test.js` 본문 읽기 / server.js 205~270 · index.html 100~212 · README.md 150~156 코드 대조 / `git log --stat`로 `test/model-compare.test.js` 삭제 확인.
- Do 기록을 신뢰한 것: 사용자 육안이 유일한 수단인 화면 SC(SC-1 · SC-2 · SC-5 · SC-6 · SC-13~SC-16 · SC-18 · SC-19 · SC-25 · SC-26). 브라우저 화면은 감사 세션이 재현할 수 없어 do §3의 날짜(2026-09-05)와 확인 내용을 신뢰하되, 각 SC가 의존하는 렌더 코드를 읽어 보조 확인했다.
- 감사 범위 밖: 아카이브 `data/fishing`의 원본 정확성(이 저장소 파일이 아니다). 활성시간 정의 자체의 타당성 — plan이 정의를 결정으로 확정했고 SC는 그 정의대로 계산되는지만 묻는다.

---

## Version History

| 버전 | 날짜 | 변경 | 계기 |
|---|---|---|---|
| 0.1 | 2026-09-05 | 감사 | Do 전 배치 검증 완료 |
